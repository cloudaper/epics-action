# Development

Install dependencies:

```shell
yarn
```

Lint:

```shell
yarn lint
```

Lint & fix:

```shell
yarn lint:fix
```

Lint & compile dependencies into `dist/index.js`:

```shell
yarn build
```
